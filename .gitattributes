# Set default behavior for text files (Git will try to auto-detect and normalize)
* text=auto

# Explicitly ensure all .sh (shell script) files use LF line endings
*.sh text eol=lf

# Example: If you had Windows batch files, you could force CRLF
# *.bat text eol=crlf

# Mark binary files to ensure Git doesn't try to convert them
*.png binary
*.jpg binary